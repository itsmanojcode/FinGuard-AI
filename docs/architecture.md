# FinGuard AI Architecture

Razorpay Test Mode -> REST/Webhooks -> Data Layer -> Reconciliation -> Anomaly -> Finance Reasoning -> Decision -> Safety Gate -> Audit -> UI.

Financial arithmetic is deterministic. The LLM explains supplied facts and does not become the source of truth for monetary calculations.
